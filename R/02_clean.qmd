---
title: "02_clean"
format: html
---

```{r}
library(tidyverse)
library(readr)
library(dplyr)
```

load the raw data

```{r}
saa_data <- read_tsv("../data/01_dat_load.tsv")
```

clean it

```{r}

```

save the cleaned data

```{r}
write_tsv(saa_data_tidy, "../data/02_dat_clean.tsv")
```
