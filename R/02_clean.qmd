---
title: "02_clean"
format: html
---

```{r}
library(readr)
library(dplyr)
```

load the raw data

```{r}
amprion_data <- read_tsv("../data/01_dat_load.tsv")
```

clean it

```{r}

```

save the cleaned data

```{r}
write_tsv(amprion_data, "../data/02_dat_clean.tsv")
```
